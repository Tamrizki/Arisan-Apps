<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tam.pa.arisanapps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArisanApps">
        <activity android:name=".activity.shake.ShakeActivity"/>
        <activity android:name=".activity.detailGroup.DetailGroupActivity" />
        <activity android:name=".activity.creategroup.CreateGroupActivity" />
        <activity android:name=".activity.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.onBoarding.OnBoardingActivity" />
        <activity android:name=".activity.home.HomeActivity" />
    </application>

</manifest>